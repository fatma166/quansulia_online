# دليل تصدير PDF مع دعم اللغة العربية

## التحديثات المطبقة

تم إصلاح مشكلة عرض اللغة العربية في ملفات PDF المصدرة من خلال التحديثات التالية:

### 1. تغيير آلية التصدير

**قبل التحديث:**
- استخدام `jspdf-autotable` مباشرة
- الخط الافتراضي (helvetica) لا يدعم العربية
- النصوص العربية تظهر كرموز غريبة: `þ®þÔþ´þßþ•`

**بعد التحديث:**
- استخدام `html2canvas` لتحويل الجدول إلى صورة
- ثم إضافة الصورة إلى PDF
- دعم كامل للغة العربية بدون أي مشاكل

### 2. الميزات الجديدة

#### ✅ دعم كامل للغة العربية
- جميع النصوص العربية تظهر بشكل صحيح
- اتجاه النص من اليمين لليسار (RTL)
- الخط: Arial (يدعم العربية بشكل كامل)

#### ✅ تقسيم تلقائي للصفحات
- إذا كان الجدول طويلاً، يتم تقسيمه تلقائياً إلى صفحات متعددة
- كل صفحة تحتوي على جزء من الجدول بشكل منسق

#### ✅ تنسيق احترافي
- عنوان التقرير: "تقرير الطلبات"
- تاريخ التقرير بالتاريخ الهجري
- جدول منسق مع ألوان متناسقة
- رأس الجدول بلون مميز (#276073)
- صفوف بديلة بألوان مختلفة

### 3. المكتبات المستخدمة

```json
{
  "html2canvas": "^1.4.1",
  "jspdf": "^3.0.4"
}
```

### 4. كيفية الاستخدام

1. افتح لوحة التحكم الإدارية
2. اذهب إلى قسم "الطلبات"
3. اضغط على زر "تصدير"
4. اختر الحقول المطلوبة
5. اختر صيغة "PDF"
6. اضغط "تصدير"

### 5. مثال على البيانات المصدرة

| الرقم المرجعي | اسم الخدمة | الحالة | اسم المتقدم | التاريخ |
|---------------|------------|--------|-------------|---------|
| SUD-2025-1234 | جواز سفر | مكتمل | أحمد محمد | 2025-02-12 |
| SUD-2025-1235 | توكيل | قيد المراجعة | فاطمة علي | 2025-02-11 |

### 6. الملفات المعدلة

- `src/components/ExportModal.jsx`

### 7. التغييرات التقنية

#### الاستيرادات
```javascript
// تم الاستبدال
import autoTable from 'jspdf-autotable';

// بـ
import html2canvas from 'html2canvas';
```

#### الدالة الرئيسية
```javascript
const exportToPDF = async () => {
  // 1. إنشاء جدول HTML مؤقت
  // 2. تحويله إلى صورة باستخدام html2canvas
  // 3. إضافة الصورة إلى PDF
  // 4. تقسيم إلى صفحات متعددة إذا لزم الأمر
}
```

### 8. الحلول المطبقة

✅ **مشكلة الترميز**: تم حلها باستخدام HTML Canvas
✅ **اتجاه النص**: RTL مطبق بشكل صحيح
✅ **الخط**: Arial يدعم العربية بالكامل
✅ **التنسيق**: احترافي ومنسق
✅ **الصفحات المتعددة**: تقسيم تلقائي للجداول الطويلة

### 9. الأداء

- سرعة التصدير: سريع جداً
- حجم الملف: متوسط (يعتمد على حجم البيانات)
- جودة الصورة: عالية (scale: 2)
- التوافق: يعمل على جميع المتصفحات الحديثة

### 10. ملاحظات هامة

⚠️ **مهم**: لا تحاول تعديل إعدادات الخط أو استخدام `doc.autoTable` مباشرة لأن ذلك سيعيد المشكلة.

✅ **موصى به**: إذا احتجت إلى تخصيص التنسيق، عدل على الجدول HTML في الدالة `exportToPDF`.

---

## الدعم الفني

إذا واجهت أي مشاكل:

1. تأكد من أن المكتبات مثبتة: `npm install`
2. امسح الذاكرة المؤقتة: `rm -rf node_modules/.vite`
3. أعد البناء: `npm run build`

---

تم التحديث في: 2026-02-12
